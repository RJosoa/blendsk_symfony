{% extends 'base.html.twig' %}

{% block title %}Like{% endblock %}

{% block body %}
    <h1>Like</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ like.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profile_like_index') }}">back to list</a>

    <a href="{{ path('app_profile_like_edit', {'id': like.id}) }}">edit</a>

    {{ include('profile_like/_delete_form.html.twig') }}
{% endblock %}
