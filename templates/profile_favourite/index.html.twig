{% extends 'base.html.twig' %}

{% block title %}Favourite index{% endblock %}

{% block body %}
    <h1>Favourite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for favourite in favourites %}
            <tr>
                <td>{{ favourite.id }}</td>
                <td>
                    <a href="{{ path('app_profile_favourite_show', {'id': favourite.id}) }}">show</a>
                    <a href="{{ path('app_profile_favourite_edit', {'id': favourite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profile_favourite_new') }}">Create new</a>
{% endblock %}
