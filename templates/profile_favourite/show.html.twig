{% extends 'base.html.twig' %}

{% block title %}Favourite{% endblock %}

{% block body %}
    <h1>Favourite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favourite.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profile_favourite_index') }}">back to list</a>

    <a href="{{ path('app_profile_favourite_edit', {'id': favourite.id}) }}">edit</a>

    {{ include('profile_favourite/_delete_form.html.twig') }}
{% endblock %}
